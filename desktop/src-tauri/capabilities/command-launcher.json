{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "command-launcher",
  "description": "Narrow remote-content capability for the trusted loopback-served command-launcher overlay. It can read host-owned drafts, route a selected contextual id, and use Desktop's normal confirm-then-quit path; source webviews cannot call its host-owned commands.",
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "windows": ["command-launcher"],
  "permissions": [
    "allow-command-launcher-overlay",
    "allow-request-app-quit",
    "core:event:default"
  ]
}
