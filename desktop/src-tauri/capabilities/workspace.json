{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "workspace",
  "description": "Capabilities for SPA webview windows: control-terminal-* are embedded terminal-only windows that run a devserver's connect script, local::<window_id> are watcher-opened embedded windows, and lib-<hex>::<window_id> are watcher-opened devserver windows. control-terminal-* needs request_close_window so Cmd+W and the not-connected close button can close the control row.",
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "windows": ["control-terminal-*", "local::*", "lib-*"],
  "permissions": [
    "workspace-window",
    "core:webview:allow-set-webview-zoom",
    "core:window:allow-set-fullscreen",
    "opener:default",
    "opener:allow-open-url"
  ]
}
