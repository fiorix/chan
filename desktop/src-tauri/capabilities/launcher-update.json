{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher-update",
  "description": "Grants the loopback-served launcher only the narrow restart-after-update app command. This replaces broad process permissions on remote content.",
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "windows": ["main", "main-*"],
  "permissions": ["allow-restart-desktop-after-update"]
}
