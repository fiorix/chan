{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-drop",
  "description": "Drag-pasteboard read for locally served local::<window_id> SPA windows only. Devserver-served lib-* windows (loopback and gateway) stay excluded because the macOS drag pasteboard is system-wide and outlives the drag.",
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "local": true,
  "windows": ["local::*"],
  "permissions": ["allow-read-dropped-paths"]
}
