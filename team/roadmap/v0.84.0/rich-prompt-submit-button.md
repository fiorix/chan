- The Rich Prompt's bottom-right hint is a non-interactive `<div class="rp-label" aria-hidden="true">` (`web/packages/workspace-app/src/components/RichPrompt.svelte:481`), so a mobile user reading "submit with cmd+enter" has no way to submit; the submit segment becomes a real button invoking `submitFromView`, the same action `Mod-Enter` runs at `:121`, and `aria-hidden` goes with it. Only that segment is a button: `labelText` (`:143`) is composite and also renders a transient note, the queued count, `esc cancel`, and the up-arrow recall and edit affordances, which are keyboard-only on mobile for the same reason and want the same treatment.
