# Terminal notifications

Status: REGISTERED for v0.87.0, deferred from v0.85.0 and again from v0.86.0; not implemented on the release ancestry.

The implementation this item once described is not on any release branch. It exists only on an abandoned candidate chain and was deliberately excluded from the v0.85.0 recovery, so v0.86.0 starts this item from the contract below rather than from working code.

- `cs notify` sends a text notification from a local machine or devserver session to eligible Chan browser and desktop surfaces.
